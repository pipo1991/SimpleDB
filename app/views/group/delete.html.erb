<%= link_to("<< Back to List", {:action => 'list'}, :class => 'back-link') %>

<div class="Group delete"</h2>

  <%= form_for(:group, :url => {:action => 'destroy',:id => @group.id}) do |grs| %>
  
  <p>Do you want to delete this Group?</p>
	
  <p class="referName"><%= @group.name%></p>
  
  <div class="from-buttons">
		<%= submit_tag("Delete Group")%>	
  </div>
  
  <% end %>
</div>
