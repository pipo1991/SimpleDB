<%= link_to("<< Back to List", {:action => 'list'}, :class => 'back-link') %>

<div class="Group update"</h2>

  <%= form_for(:group, :url => {:action => 'update',:id => @group.id}) do |grs| %>
  
  <table summary="Group detail view">
  	<tr>
      <th>Group name</th>
      <td><%= grs.text_field(:name) %></td>
    </tr>
  </table>
  
  <div class="from-buttons">
		<%= submit_tag("Update Group")%>	
  </div>
  
  <% end %>
</div>
